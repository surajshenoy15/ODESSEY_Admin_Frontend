# Admin Frontend Validation

- JS/JSX syntax was parsed successfully across all source files.
- Registration review layout includes a left-side bonafide viewer and right-side student/profile-photo roster.
- Attendance page supports QR token query loading and blocks repeat confirmation after a recorded check-in.
- Certificate page includes audited manual eligibility override for a missed participant.
- Events page supports SPORTS/CULTURAL event type and event poster upload.

Run `npm install && npm run build` in your target development environment to perform the final Vite production build with platform-native optional dependencies.
